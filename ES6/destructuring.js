// với array
const colors=["red","green","blue"];
const [firstColor,secondColor,thirdColor]=colors;

// với object
const myInfo={
    id:1,
    userName:"NVA",
    age:"22"
}

const {age,id,userName}=myInfo;

// Destructuring dùng để truy xuất phần tử trong mảng và object dễ dàng